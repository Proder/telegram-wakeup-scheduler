{
  "name": "telegram-wakeup-scheduler",
  "version": "1.0.0",
  "description": "Advanced wake-up call scheduler service for Telegram bot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "telegram",
    "wakeup",
    "scheduler",
    "alarm",
    "cron"
  ],
  "author": "Proder",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.10.0",
    "express": "^4.21.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
